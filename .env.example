# FastAPI Finance Monitor — Пример файла переменных окружения
# Скопируйте этот файл в .env и настройте под свой проект
# ВАЖНО: Никогда не коммитьте файл .env в репозиторий ...

# ===================
# Настройки сервера #
# ===================
HOST=0.0.0.0                    # Сетевой интерфейс (0.0.0.0 — все интерфейсы)
PORT=8000                       # Порт (int)
RELOAD=True                     # Автоперезагрузка при разработке (bool: True/False)

# =================================
# Обновление и кэширование данных #
# =================================
UPDATE_INTERVAL=30              # Интервал обновления данных в секундах (int)
MAX_CHART_POINTS=100            # Макс. число точек на графике (int)
CACHE_TIMEOUT=10                # Время кэширования ответов в секундах (int)

# ========================
# Технические индикаторы #
# ========================
RSI_PERIOD=14                   # Период RSI (int)
RSI_OVERBOUGHT=70.0             # Уровень перекупленности (float)
RSI_OVERSOLD=30.0               # Уровень перепроданности (float)

SMA_SHORT_PERIOD=20             # Короткий период SMA (int)
SMA_LONG_PERIOD=50              # Длинный период SMA (int)

EMA_FAST_PERIOD=12              # Быстрый период EMA (int)
EMA_SLOW_PERIOD=26              # Медленный период EMA (int)

MACD_FAST=12                    # Быстрая EMA для MACD (int)
MACD_SLOW=26                    # Медленная EMA для MACD (int)
MACD_SIGNAL=9                   # Период сигнальной линии MACD (int)

# =============================
# Источники финансовых данных #
# =============================
YAHOO_FINANCE_PERIOD=1d         # Период истории Yahoo Finance (1d, 5d, 1mo и т.д.)
YAHOO_FINANCE_INTERVAL=5m       # Интервал свечей (1m, 2m, 5m, 15m, 1h, 1d и т.д.)

COINGECKO_DAYS=7                # Дней истории для криптовалют (int или 'max')
COINGECKO_INTERVAL=hourly       # Интервал: 'hourly', 'daily'

# ======================================
# Ограничение запросов (Rate Limiting) #
# ======================================
MAX_REQUESTS_PER_MINUTE=60      # Макс. число запросов с IP в минуту (int)

# ============================
# CORS (межсайтовые запросы) #
# ============================
# ⚠️ В продакшене НИКОГДА не используйте ALLOWED_ORIGINS=*
# Укажите конкретные домены, например: https://myapp.com,https://dashboard.example.org
ALLOWED_ORIGINS=*               # Разрешённые источники (разделяйте запятыми)

# =============
# Логирование #
# =============
LOG_LEVEL=INFO                  # Уровень логов: DEBUG, INFO, WARNING, ERROR, CRITICAL

# ===================================================
# Redis (опционально, для кэширования или очередей) #
# ===================================================
# REDIS_URL=redis://localhost:6379/0

# ====================================
# Уведомления по email (опционально) #
# ====================================
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password  # Используйте "пароль приложения", а не основной!

# ==========================================
# Уведомления через Telegram (опционально) #
# ==========================================
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHAT_ID=your-chat-id

# ==========================================
# API-ключи платных сервисов (опционально) #
# ==========================================
# ALPHA_VANTAGE_KEY=your-key
# FINNHUB_KEY=your-key
# POLYGON_KEY=your-key